# This is a test fixture mimicing an Apache .htaccess config file.

Header set "access-control-allowed-origin" "*"

# BEGIN replaceme

I have nothing that binds me to this binary realm anymore...


# END replaceme


# BEGIN unrelated

# This bit should remain untouched
RewriteBase /
RewriteCond %{REQUEST_FILENAME} *
RewriteRule . / [N,R]


# END unrelated

"Some content in between"

# BEGIN removal

I won't be here forever.

# END removal
